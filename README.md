# Offline-ChatWithPDF
ChatPDF — Local Python app for conversational PDF search. Load and index PDFs, then ask questions via a simple UI (mainui.py). Lightweight dependencies listed in requirements.txt. Uploaded with permission; see README for attribution and chosen license.
